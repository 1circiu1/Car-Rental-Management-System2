<Page
    x:Class="Project.Views.AdminDashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="Transparent">

    <Grid Padding="40">
        <StackPanel Spacing="24">

            <TextBlock Text="Admin Dashboard"
                       FontSize="32"
                       FontWeight="SemiBold"/>

            <TextBlock Text="Manage cars, customers and reservations."
                       FontSize="16"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Padding="24" CornerRadius="16"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel>
                        <TextBlock Text="Cars" FontSize="18" FontWeight="SemiBold"/>
                        <TextBlock Text="View and manage all cars"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Padding="24" CornerRadius="16"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel>
                        <TextBlock Text="Customers" FontSize="18" FontWeight="SemiBold"/>
                        <TextBlock Text="View registered customers"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Padding="24" CornerRadius="16"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel>
                        <TextBlock Text="Reservations" FontSize="18" FontWeight="SemiBold"/>
                        <TextBlock Text="Manage reservations"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Button Content="Sign out"
                    Width="140"
                    Click="SignOut_Click"/>
        </StackPanel>
    </Grid>
</Page>