<Page
    x:Class="Project.Views.Dashboard.Notifications.NotificationsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="Transparent">

    <ScrollViewer>
        <StackPanel Spacing="24">

            <StackPanel Spacing="6">
                <TextBlock Text="Notifications"
                           FontSize="32"
                           FontWeight="SemiBold"/>

                <TextBlock Text="Stay updated with reservations, payments and support activity."
                           FontSize="16"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>

            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Padding="22"
                        CornerRadius="16"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">

                    <StackPanel Spacing="8">
                        <TextBlock Text="Unread notifications"/>

                        <TextBlock x:Name="UnreadCountText"
                                   Text="0"
                                   FontSize="32"
                                   FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1"
                        Padding="22"
                        CornerRadius="16"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">

                    <StackPanel Spacing="8">
                        <TextBlock Text="Total notifications"/>

                        <TextBlock x:Name="TotalNotificationsText"
                                   Text="0"
                                   FontSize="32"
                                   FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Padding="24"
                    CornerRadius="16"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">

                <StackPanel Spacing="18">

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="Recent notifications"
                                   FontSize="22"
                                   FontWeight="SemiBold"/>

                        <Button Grid.Column="1"
                                Content="Mark all as read"
                                Click="MarkAllAsRead_Click"/>
                    </Grid>

                    <StackPanel x:Name="NotificationsPanel"
                                Spacing="14"/>

                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>